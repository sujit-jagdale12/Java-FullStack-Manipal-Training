spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ormtraining
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
       hibernate:
        format_sql: true
